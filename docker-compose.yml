version: "3.9"

services:

  backend:
    build: ./server
    container_name: iut-backend
    ports:
      - "5000:5000"

  frontend:
    build: ./frontend
    container_name: iut-frontend
    ports:
      - "5173:80"
    depends_on:
      - backend